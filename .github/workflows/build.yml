name: Build

on:
  push:
    branches: [main, testing]
  pull_request:
    branches: [main, testing]
  workflow_dispatch:
    inputs:
      image:
        description: 'Image to build (plateau, strata, all)'
        required: false
        default: 'all'
        type: choice
        options: [all, plateau, strata]
      variant:
        description: 'Variant to build (default, gaming)'
        required: false
        default: 'default'
        type: choice
        options: [default, gaming]
      x86_64_v3:
        description: 'Enable x86_64-v3 optimizations'
        required: false
        default: 'true'
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_ORG: huntedraven7

jobs:
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            plateau:
              - 'elements/plateau/**'
              - 'files/**'
              - 'include/**'
              - 'Justfile'
              - 'project.conf'
              - 'Containerfile'
            strata:
              - 'elements/strata/**'
              - 'files/**'
              - 'include/**'
              - 'Justfile'
              - 'project.conf'
              - 'Containerfile'
            core:
              - 'elements/core/**'
              - 'elements/oci/**'
              - 'elements/plugins/**'
              - 'plugins/**'
              - 'include/**'
              - 'Justfile'
              - 'project.conf'
            nvidia:
              - 'elements/bluefin-nvidia/**'

      - name: Set matrix
        id: set-matrix
        run: |
          # Determine which images to build based on workflow dispatch or changed paths
          IMAGES=""
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            case "${{ github.event.inputs.image }}" in
              all) IMAGES="plateau strata" ;;
              plateau) IMAGES="plateau" ;;
              strata) IMAGES="strata" ;;
            esac
          else
            # Auto-detect from changed paths
            IMAGES=""
            if [ "${{ steps.filter.outputs.plateau }}" = "true" ] || [ "${{ steps.filter.outputs.core }}" = "true" ]; then
              IMAGES="$IMAGES plateau"
            fi
            if [ "${{ steps.filter.outputs.strata }}" = "true" ] || [ "${{ steps.filter.outputs.core }}" = "true" ]; then
              IMAGES="$IMAGES strata"
            fi
            # If no specific changes, build all on main/testing push
            if [ -z "$IMAGES" ] && [ "${{ github.event_name }}" = "push" ]; then
              IMAGES="plateau strata"
            fi
          fi

          # Determine variants
          VARIANTS=""
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            case "${{ github.event.inputs.variant }}" in
              default) VARIANTS="default" ;;
              gaming) VARIANTS="gaming" ;;
            esac
          else
            VARIANTS="default"
            if [ "${{ steps.filter.outputs.nvidia }}" = "true" ]; then
              VARIANTS="gaming"
            fi
          fi

          # Determine archs - Plateau uses x86_64_v3, Strata uses x86_64 only
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # For dispatch, use the input x86_64_v3 boolean
            if [ "${{ github.event.inputs.x86_64_v3 }}" = "true" ]; then
              X86_64_V3_VALUES="true"
            else
              X86_64_V3_VALUES="false"
            fi
          fi

          # Build matrix JSON using jq to ensure proper escaping
          MATRIX='{"include":[]}'
          for image in $IMAGES; do
            for variant in $VARIANTS; do
              # Strata doesn't have gaming variant
              if [ "$image" = "strata" ] && [ "$variant" = "gaming" ]; then
                continue
              fi
              # Plateau uses x86_64_v3, Strata uses x86_64 only
              if [ "$image" = "plateau" ]; then
                X86_64_V3_VALUES_IMAGE="false true"
              else
                X86_64_V3_VALUES_IMAGE="false"
              fi
              # Override for workflow dispatch
              if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ github.event.inputs.x86_64_v3 }}" = "true" ]; then
                X86_64_V3_VALUES_IMAGE="true"
              fi
              for x86_64_v3 in $X86_64_V3_VALUES_IMAGE; do
                MATRIX=$(echo "$MATRIX" | jq --arg img "$image" --arg var "$variant" --arg v3 "$x86_64_v3" '.include += [{"image": $img, "variant": $var, "x86_64_v3": $v3}]')
              done
            done
          done

          # Escape for GITHUB_OUTPUT
          echo "matrix<<EOF" >> $GITHUB_OUTPUT
          echo "$MATRIX" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  build:
    name: Build ${{ matrix.image }}-${{ matrix.variant }}-v3${{ matrix.x86_64_v3 }}
    needs: detect-changes
    if: needs.detect-changes.outputs.matrix != '{"include":[]}'
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup runner
        uses: projectbluefin/actions/bootc-build/setup-runner@v1
        with:
          install-tools: '["just", "cosign", "oras", "skopeo"]'

      - name: Build image
        run: |
          just bst -o arch x86_64 \
            -o gaming ${{ matrix.variant == 'gaming' && 'true' || 'false' }} \
            -o x86_64_v3 ${{ matrix.x86_64_v3 }} \
            build ${{ matrix.image }}/${{ matrix.image }}.bst

      - name: Generate tags
        id: tags
        uses: projectbluefin/actions/bootc-build/generate-tags@v1
        with:
          image: ${{ matrix.image }}
          stream: ${{ github.ref_name == 'main' && 'stable' || 'testing' }}
          version: 26.08.1

      - name: Push image
        if: github.event_name != 'pull_request'
        uses: projectbluefin/actions/bootc-build/push-image@v1
        with:
          image: ${{ steps.tags.outputs.image }}
          tags: ${{ steps.tags.outputs.tags }}
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create manifest
        if: github.event_name != 'pull_request'
        uses: projectbluefin/actions/bootc-build/create-manifest@v1
        with:
          image: ${{ steps.tags.outputs.image }}
          tags: ${{ steps.tags.outputs.tags }}
          registry: ${{ env.REGISTRY }}

      - name: Sign and publish
        if: github.event_name != 'pull_request' && github.ref_name == 'main'
        uses: projectbluefin/actions/bootc-build/sign-and-publish@v1
        with:
          image: ${{ steps.tags.outputs.image }}
          tags: ${{ steps.tags.outputs.tags }}
          registry: ${{ env.REGISTRY }}
          cosign-password: ${{ secrets.COSIGN_PASSWORD }}
          cosign-private-key: ${{ secrets.COSIGN_PRIVATE_KEY }}

      - name: Chunkah rechunking
        if: github.event_name != 'pull_request'
        uses: projectbluefin/actions/bootc-build/chunka@v1
        with:
          image: ${{ steps.tags.outputs.image }}
          tags: ${{ steps.tags.outputs.tags }}
          registry: ${{ env.REGISTRY }}

      - name: Scan image
        if: github.event_name != 'pull_request'
        uses: projectbluefin/actions/bootc-build/scan-image@v1
        with:
          image: ${{ steps.tags.outputs.image }}
          tags: ${{ steps.tags.outputs.tags }}
          registry: ${{ env.REGISTRY }}
          severity: HIGH,CRITICAL

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.image }}-v3${{ matrix.x86_64_v3 }}-${{ matrix.variant }}
          path: |
            dist/
          retention-days: 7